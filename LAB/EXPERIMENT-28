q = 23
a = 5

x = 6
y = 15

A = pow(a, x, q)
B = pow(a, y, q)

key_alice = pow(B, x, q)
key_bob = pow(A, y, q)

print("Alice sends:", A)
print("Bob sends:", B)
print("Shared key (Alice):", key_alice)
print("Shared key (Bob):", key_bob)
